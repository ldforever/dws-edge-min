# 2026-09-22

## 项目熟悉
通读 HANDOFF.md / README.md / docs\操作手册.md，沉淀到 `.workbuddy/memory/MEMORY.md`（架构不变量、离线硬约束、现场铁律、关键配置位置）。

## 前端 UE 梳理（未改代码）
只读 `src\DwsEdge.Platform\wwwroot\index.html` 摸清界面结构后，产出一份重排方案并以内联线框图交付，未改动任何项目文件。

现状问题（已记录，待用户确认后决定是否落地）：
1. 10 个页签平铺，场景分组（监控 / 配置 / 系统）在 UI 上没有体现。
2. 术语泄漏：`相机状态墙（C2）`、`下游输出（TCP，B4）`、`日志查看与导出（C6）`、`配置模板（A8-3）` 等需求编号出现在页面上（HANDOFF P1 已列此条）。
3. 板块错位：去重指纹归档、监控与告警阈值都在「规则」页；配置模板在「相机」页；班次设置在「统计」页。
4. 全局「一键应用」条在所有页签常驻，非配置页纯占位。
5. 实时监控页四层上下堆叠（KPI/告警/过包墙/相机墙），大屏一屏看不全。

建议方案：三分区 IA + 板块归位（阈值→设备页、模板与去重→系统与安全页）+ 实时监控改左右分栏 + 应用条仅在配置类页常驻 + 文案去编号。

## 约定
用户明确要求「先让我看一下，不要操作项目代码」时，只做只读探索与内联交付，不写项目文件。
