# DWS 物流解码平台 · 前端改造任务清单

> 目标：把"能用"的界面改成"现场好用"的界面。**只动前端**。
> 适用对象：改界面的人（开发），以及验收的人（现场 / 产品）。
> 配套：`README.md`（架构）、`docs\操作手册.md`（现场操作）、`HANDOFF.md`（接手入口）
> 对照基准：桌面 `dws-ui-redesign.html`（可交互设计图，能切页面 / 导航形态 / 主题 / 信息密度）
> 最后更新：2026-09-22 —— 新增 T0.5（侧边栏）、T0.6（品牌与图标），并补上附录 A 的逐项对照

---

## 0. 为什么要做这一轮

界面本身功能是齐的（24 项需求都实现过），问题是**信息架构**，不是功能缺失。实际使用中反复出现的四件事：

| # | 现象 | 根因 |
|---|---|---|
| 1 | 想改个存图天数，在"配置"页里翻了半天 | 一个页签塞了 10 个板块、50 多个输入框，没有分组 |
| 2 | 现场问"为什么不出包"，要在 4 个页签之间来回跳着看 | 宿主 / 通道 / 相机 / 下游 / 磁盘 分散在不同页 |
| 3 | 客户看不懂"应用 A8-3 模板""B8 监控" | 标题里带着需求编号 |
| 4 | 报错弹窗挡住整个界面，扫包时一直弹 | 早期用 `window.alert`（已改成右下角 toast，仍有残留文案） |

**这一轮就是把 1 和 2 解决掉，顺带把 3 和 4 收口。**

---

## 1. 三条不能破的线

这三条是本清单的"红线"，任何一个任务包违反就算不合格：

1. **元素 id 一个都不能改。** 19 个回归脚本靠 `id="…"` 断言（`tab-*`、`hostState`、`camEditRows`、`btnApply`、`auth*`…）。要改名必须同步改回归脚本，那就不属于"只动前端"了。
2. **不新增后端接口，不改接口字段。** 前端只能重组现有接口的调用方式。代价是某些功能要用"组合现有接口"实现（例如 T6 的前端版一键自检）。
3. **改完必须跑 `node build.mjs`。** 它会把 `js\*.js` + `app.css` 的内容算成指纹写进 `index.html`（`?v=xxxxxxxx`），不跑的话现场浏览器会拿缓存，改了看不到效果。

---

## 2. 任务包总览

共 10 个任务包，合计约 **10.5 人天**。按四个迭代交付，每个迭代都能单独上线、单独回滚。

| # | 任务包 | 改哪些文件 | 工作量 | 状态 |
|---|---|---|---|---|
| **T0** | 外壳与分组导航 + 全局应用条 + 主题/密度（**只做了分组，形态仍是页头横排**） | `index.html`、`shell.ts`、`main.ts`、`styles.css` | 1 天 | ✅ 已完成 |
| **T0.5** | **左侧侧边栏导航 + 形态切换（侧栏 / 顶部标签）** | `index.html`、`nav.ts`、`styles.css`、`main.ts` | 0.5 天 | 待做（**优先级最高**） |
| **T0.6** | 顶条品牌区（logo / 公司名 / 版本）+ 导航图标 + 导航角标 | `index.html`、`styles.css`、`icons.ts` | 1 天 | 待做 |
| **T1** | 页头状态条（宿主/通道/相机/下游/磁盘） | `statusbar.ts`、`api.ts`、`auth.ts` | 0.5 天 | ✅ 已完成 |
| T2 | 实时监控页打磨（KPI 补过包节奏、告警分级、卡片与相机墙信息层级） | `realtime.ts` | 2 天 | 待做 |
| T3 | 相机页独立（从 `config.ts` 拆出 `cameras.ts`） | `cameras.ts` | 1.5 天 | 待做 |
| T4 | 输出对接页 + 规则页各自独立 | `downstream.ts`、`rules.ts`、`monitor.ts` | 1 天 | 待做 |
| T5 | 系统与安全页 | `auth.ts` | 0.5 天 | 待做 |
| T6 | 诊断页升级（前端版"一键自检" + 日志跟随） | `selfcheck.ts`、`diag.ts` | 1.5 天 | 待做 |
| T7 | 文案与反馈统一 + 通用两栏布局组件 | 全前端 + `index.html` | 1 天 | 待做 |

**迭代划分**

- 迭代 ①（已交付）：T0 + T1 — 分组导航、状态条。**已完成。**
- 迭代 ①.5（下一步）：**T0.5 + T0.6** — 侧边栏形态 + 顶条品牌与图标。它只动布局，不动任何数据逻辑，是"最显眼、最便宜"的一次升级；而且两块代码挨着（顶条 / 导航），分两次改反而更费。
- 迭代 ②：T2 + T3 + T4 + T5 — 四个页签各自打磨，互不依赖，可以并行。
- 迭代 ③：T6 + T7 — 自检、日志与文案，放最后（要覆盖前面所有页面）。

> 逐项对照设计图（哪一条落在哪个任务包、还差什么）见 **附录 A**；与设计图有意不一致的地方见 **附录 B**。

---

## 3. 任务包详情

### T0 外壳与分组导航（✅ 已完成，V1.0.9 热修）

**做了什么**

- 导航从"一排 9 个按钮"改成三组，组标签是纯文字：**监控**（实时监控 / 设备信息 / 历史查询 / 统计 / 诊断）、**配置**（相机 / 输出对接 / 规则）、**系统**（系统与安全）。
- 页头右侧加"显示设置"：主题（深色 / 浅色 / 跟随系统）+ 信息密度（舒适 / 紧凑）。存在浏览器 `localStorage`，是"这台机器这个人"的偏好，不进后端配置。
- 颜色全部 token 化（190 处写死色值 → CSS 变量），深色观感逐色一一对应，**外观不变**。
- 密度 token 接进 `header` / `main` / `.card` / `.panel` / `th,td` / `.applybar`：紧凑模式下字号 13→12px、单元格内距 8/10→5/8px。
- 默认主题是**深色**（和原来一致）。默认给"跟随系统"的话，Windows 处于浅色模式的机器一升级整个界面就变白 —— 那是惊吓不是升级。

**验收（已实测）**

| 项 | 结果 |
|---|---|
| 三组导航都在，9 个 `tab-*` 一个不少 | ✅ 结构体检 11 项全 PASS |
| 浅色 + 紧凑：`data-theme=light`、`font-size=12px`、背景 `rgb(246,248,250)` | ✅ 浏览器实测 |
| 深色 + 舒适：`data-theme=dark`、`font-size=13px`、背景 `rgb(18,22,28)` | ✅ 浏览器实测 |
| 刷新后主题不闪（`index.html` 的 `<head>` 里先贴 `data-theme` 再加载样式） | ✅ |

**注意**：`index.html` 里那段 6 行的开机脚本和 `shell.ts` 共用两个 key（`dws.ui.theme` / `dws.ui.density`），改一个必须改另一个。T0.5 加"导航形态"时也要一起加。

---

### T0.5 左侧侧边栏导航（0.5 天，**下一步就做这个**）

**要解决的问题**：设计图默认就是左侧竖栏，现在落地的是页头横排。T0 把"分类"做对了（监控 / 配置 / 系统三组），但"形态"没动 —— 9 个页签横排在大屏上还能挤下，再加页签就会把顶条撑爆；而且分组标签挤在横排里只是一小行灰字，起不到"分区"的作用。

**做这些**

1. `header` 瘦身成"顶条"：只留品牌区（T0.6 做）+ 状态条 + 用户区 + 显示设置，不再放导航。
2. 主体改成两栏：加一层 `.body`（`display:flex`），左边 `<nav>` 固定 **178px**（窄屏 ≤860px 时 150px），右边 `<main>` 自适应。
3. 导航项改成整行按钮、左对齐，当前项用"浅底 + 描边"（现在用的是实心蓝，在大屏上太重、太抢）。组标签（监控 / 配置 / 系统）纵向排列在每组上方。
4. **形态可切换**：加 `data-nav="left|top"`，和主题 / 密度同一套做法写 `localStorage`（key 建议 `dws.ui.nav`），**默认 `left`**。设计图就是这么做的（它的"导航形态"选择器有 左侧栏 / 顶部标签 两个值）——现场大屏用侧栏，临时接笔记本时切顶部标签，不用改代码。
5. `index.html` 的 `<head>` 那段开机脚本要一起改：`data-nav` 也得在样式加载前贴上，否则刷新时导航会先横排、再跳成侧栏。

**硬约束（这条最容易踩）**：9 个 `tab-*` 的 id 一个都不能改；`#applyBar` 必须仍在 `</header>` 之后、`<main>` 之前（`test-ui-structure.ps1` 正是拿这三个位置关系断言的）。

**验收（结构体检测不出来，必须人工看）**

| 项 | 怎么验 |
|---|---|
| 侧栏在左、内容在右，不重叠、不抖动 | 浏览器里看，1280 与 1920 宽各截一张图 |
| 切到"顶部标签"形态和现在一致 | 点一下切换开关对比 |
| 9 个页签都能点、能切页 | 挨个点一遍 |
| 应用条仍在顶条下方、内容上方 | 结构体检 11 项 + 肉眼 |
| 刷新后形态不闪 | 选侧栏 → F5 → 不应先横排再变竖排 |
| 窄屏（≤860px）侧栏缩到 150px、内容不被挤没 | 浏览器拉到 800px 宽看 |

---

### T0.6 顶条品牌区 + 导航图标 + 导航角标（1 天）

**做这些**

1. **品牌区**：22px 圆角方块 logo（主色底 + 条码图标）+ 产品名 + 版本号小字。设计图写的是"莱茨酷氪 DWS 物流解码平台 · V1.0.9 · 六面扫"；现在顶条只有一句"采集监控 · 设备信息 · 配置管理（V1）"。
   - 版本号来源二选一，**都不用改后端**：写在 `index.html`（发版时手改，最省事），或读 `/api/health` 已有字段。选哪个要写在这里：☐ 未定。
2. **导航图标**：设计图每个导航项都有 16px 图标。**现场是离线环境，不能用 CDN** —— 要么把 SVG 内联进 `icons.ts`，要么把图标字体文件随 `wwwroot` 一起发布。这条属于"决策 + 任务"：不做也行（纯文字也能用），但"不做"的决定要写下来（进附录 B）。
3. **导航角标**：设计图在"相机"这一项右侧挂了个数字角标（`17`）。建议用法：**设备信息**挂未处理告警数、"相机"挂清单台数，数据用已有的 `/api/monitor/*`、`/api/devices`，不新增接口。
4. 用户区样式统一（"admin（管理员）" + 退出按钮），与状态条同一行右对齐。

**验收**：结构体检 11 项 + 人工截图；角标数字与 `/api/devices`、`/api/monitor/summary` 对得上。

---

### T1 页头状态条（✅ 已完成，V1.0.9 热修）

**做了什么**

页头常驻 5 个状态胶囊 + 1 个实时通道，5 秒刷一次。这六件事任意一个不满足都"出不了包"，所以放在最显眼的位置：

| 胶囊 | 数据来源 | 判定 |
|---|---|---|
| 采集宿主 | `GET /api/host/status` | running→绿；retrying→黄（显示第几次重试）；其它→红 |
| 命令通道 | `GET /api/host/channel` | 可用→绿；宿主在跑但通道断→红；宿主没跑→黄（不跟着再报一次红） |
| 相机 | `GET /api/devices` | 全部在线→绿；部分在线→黄；全掉→红；清单为空→灰 |
| 下游 | `GET /api/downstream` | 未启用→灰；TCP 已连接 / HTTP 推送 / 服务端有人连→绿；未连或失败→黄 |
| 图片盘 | `GET /api/stats` | <70%→绿；≥70%→黄；≥85%→红（附"超过 85% 会造成丢图"） |
| 实时通道 | SSE `onStatus` | 连上→绿；重连中→红 |

**轮询的三条规矩（很重要，别改回去）**

1. **没登录就不发请求。** 用 `auth.ts` 新导出的 `canRead()` 判断（已登录 / 没开鉴权 / 只读接口没受保护，三种才算"能读"）。不在这个门后面拦住的话，后台轮询拿到 401 → `onUnauthorized` → 弹登录框 → **把用户正在输密码的光标抢回用户名框**（这是历史上真实踩过的坑）。
2. **下游状态单独走一个"安静版"入口** `api.downstreamQuiet()`。`/api/downstream` 是受保护接口（同路由有 POST），未登录会 401；`downstreamQuiet` 打同一个接口但 `silent401`，只有它用于状态条。输出对接页仍然用原来的 `api.downstream()`。
3. **单个接口失败只染它自己那一个胶囊**，不会把整条状态条拖红；文字没变就不写 DOM。

**验收（已实测）**

| 项 | 实测值 |
|---|---|
| 宿主 / 通道 / 相机 / 磁盘胶囊取到真值 | `采集宿主：运行中`(绿)、`命令通道：可用`(绿)、`相机：1/1 在线`(绿)、`图片盘：92%`(红) |
| 未登录时下游胶囊 | `下游：登录后显示`（灰），且**没有弹登录框** |
| 页面加载时不再出现"登录已过期"误报 | ✅（状态条不再产生 401） |

---

### T2 实时监控页打磨（2 天）

**要解决的问题**：实时页现在 KPI 只有 4 个、告警挤在一条灰条里、卡片和相机墙的徽标样式各写各的。

**做这些**

1. KPI 补到 5 个：包裹总数（今日）/ 读码率 / 无码包裹 / 相机在线 / **平均过包节奏**。
   - 单位定成 **每秒**（设计图写的是"2.1 /s"；本条原来写的是"每分钟包数"，改成每秒 —— 和现场节拍同量级，不用在脑子里换算），保留一位小数。
   - 用 `parcels` 的最近 N 条按时间戳前端算，不新增接口。
   - 布局改成**自适应网格** `repeat(auto-fit, minmax(140px, 1fr))`：现在是写死 4 列 / 5 列，窗口一窄就挤、一宽就空一大片。
2. 告警条分级，**并且"没告警"时也要有话说**：critical（清单里有但没发现、相机全掉）红底、warning（心跳超时、在线率低）黄底；一条都没有时不要留空，显示绿底"相机与下游均正常 · 近 10 分钟没有掉线或告警"（设计图就是这么画的）。点开进设备页。
3. **包裹卡片的信息层级定稿**（设计图给的就是这一套，建议照抄）：
   - 缩略图 76×60、固定在左边；
   - 条码用等宽字体（Consolas 一类）—— 现场对单号时数字宽度一致，肉眼好核对；
   - 一行元信息：`时间 · 方位 · 相机 · 重量`（现在没有**重量**，而它是两次回调合并后才有的数据，别浪费）；
   - 三个标签：读码状态（已读码 / 无码）、`图 N`、下发状态（已下发 / 下发中 / 失败）；"更新 ×2"表示两次回调合并过。
4. 卡片墙容器改成 `repeat(auto-fill, minmax(232px, 1fr))`，卡片宽度别再写死。
5. **相机状态墙**：每张卡左边加 3px 状态色条（绿 / 黄 / 红），一眼扫出异常；卡内第一行 `相机名 + 方位` 与右侧状态词（在线 / 心跳慢 / 离线 / 未发现），第二行 `出码 N · 在线率 N%`。从没触发过的相机显示"出码 0 · 未触发"，**别显示 0%**（会被当成故障）。
6. 徽标统一到一个 `badge()` 出口（`dom.ts` 已有），把实时页里手写的 `<span class="tag">` 收口。
7. 卡片墙空态："还没有包裹 · 点「软触发一次」试一下"。

**验收**：C1 卡片墙 28 项、C2 状态墙 29 项回归全绿 + 人工截图（1920 与 1280 各一张）。

---

### T3 相机页独立（1.5 天，搬运 + 重新排布）

**要解决的问题**：`config.ts` 965 行，相机清单、存图策略、配置模板、一键应用全挤在一起，改一处要读一整个文件。

**做这些**

1. 新建 `cameras.ts`，把相机清单表格、方位映射、扫描/预检、SDK 配置摘要搬过去；`config.ts` 只留存图策略与模板。
2. 相机页顺序：**清单（表格编辑）→ 存图策略与配置模板（并排两栏）→ SDK 配置摘要**。
3. 清单上方四件套工具按钮（设计图有，清单原来没写）：**扫描在线相机**、**连通性预检**、**新增一行**、**校验清单格式**。后两个是纯前端动作（改表格 / 走已有校验接口），前两个分别对应已有的扫描与 `/api/camera-probe`。
4. 表格下一行说明文字，把"为什么按 IP 写最省事"讲清楚：清单按 IP 写时，宿主会自动和 SDK 上报的「厂商:序列号」关联（对照表 `config\camera-identity.ini`）。现场最常问的就是这个。
5. 表格列定稿：`# / 接入方式 / 值（IP·序列号·id）/ 方位 / 预检 / 操作`。**预检单独成列**，每行给三种状态文案（与状态条同色系）：
   - 绿：`在线，SDK 已发现（1ms）`
   - 黄：`能 ping 通，但 SDK 没发现`
   - 红：`ping 不通（没上电 / 网段不对）`
6. 页面底部"存图策略"和"配置模板"两张卡**并排**（`1.25fr / 1fr`，≤860px 时自动堆成上下），不要上下堆一大串。

**验收**：C5 配置页 61 项（相机清单那一段必须仍然 PASS）、A8 模板 49 项。

---

### T4 输出对接页 + 规则页独立（1 天）

**做这些**

1. 输出对接页：协议（TCP 客户端 / TCP 服务端 / HTTP）用单选卡片切换，只显示当前协议相关的字段（现在是三套字段全摆着）。
2. 模板编辑区左边编辑、右边实时预览（调 `previewDownstream`，已有接口）。
3. 规则页：把过滤规则 + 去重 + 监控阈值三块分区，每块独立"保存"。

**验收**：B4 / B5 / B6 / B2 回归全绿。

---

### T5 系统与安全页（0.5 天）

**做这些**：账号列表、角色说明、改密、服务令牌轮换、审计日志查询分区；"最后一个管理员不能删"这类约束在按钮上就灰掉，不要等点了才报错。

**验收**：B9 鉴权 96 项全绿。

---

### T6 诊断页升级：前端版"一键自检" + 日志跟随（1.5 天）

**要解决的问题**：现在 `self-check.ps1` 只在命令行跑，界面上要一项项点。

**做这些**

1. 新增 `selfcheck.ts`，**用现有接口组合**出一条检查清单，逐项显示结果 + 说明：

| 检查项 | 用什么接口 |
|---|---|
| 平台在线 | `GET /api/health` |
| 采集宿主在跑 | `GET /api/host/status` |
| 命令通道可用 | `GET /api/host/channel` |
| 相机清单 vs 实际发现 | `GET /api/devices` |
| SDK 配置回读一致 | `GET /api/config` |
| 下游连通 | `GET /api/downstream` |
| 图片盘水位 | `GET /api/stats` |
| 最近 5 分钟解析失败 | `GET /api/stats` 的 `parseErrors` |
| 日志可打包 | `GET /api/diag/*` |

2. **三态而不是两态**：通过 / 失败 / **跳过**。典型场景：相机不可用，那"软触发"这一项自动跳过，不要显示成失败（设计图里就是 `跳过 · 相机不可用，自动降级跳过`）。
3. **失败项要带处置动作**，不要只说哪里错。例："加密狗与相机 — 失败 — SDK 返回 3000：没有相机连上（给相机断电重上电后重试）"。这一条是自检页最值钱的地方：现场的人照着做就行，不用打电话。
4. 顶部工具栏：`一键自检`（主按钮）+ `导出日志包` + **上次自检摘要**（"上次自检：今天 14:28 · 通过 5 项，失败 1 项（相机）"）。
5. 页面左右两栏（`1.25fr / 1fr`，≤860px 堆成一栏）：**左边自检结果表**，**右边实时日志**（来源下拉 + "跟随最新"开关）。现在日志要跳到另一个区域去看，排障时来回滚。
6. 最后给一个"复制自检结果"按钮（整张表复制成纯文本，现场直接发微信）。

**验收**：C6 诊断 49 项 + 人工在仿真模式跑一遍（`provider=simulator`）。

---

### T7 文案与反馈统一 + 通用两栏布局（1 天）

**做这些**

1. 删掉界面上的需求编号（"C2 相机状态墙"→"相机状态"、"B8 监控"→"相机监控"、"A8-3 模板"→"配置模板"）。
2. 术语定稿（界面 = 中文人话，代码/日志可以保留术语）：采集宿主 / 业务平台 / 相机清单 / 一键应用 / 图片盘 / 命令通道。
3. 空态统一：无数据一律"—"；列表空一律"暂无数据 ·（下一步怎么做）"。
4. 反馈统一：`toast()` 管提示、`confirmBox()` 管二次确认；全项目不再有 `window.alert` / `confirm`（现在应该已经没有了，用 `grep` 守一下）。
5. 错误文案补"下一步"："校验失败，已自动回滚" → "校验失败，已自动回滚到应用前的配置。请先看「诊断」页的相机预检"。
6. 应用条补"**上次应用：几分钟前 · 成功（用了 N 秒）**"（附录 A 第 8 条）—— 结果要落盘或记在内存里，刷新页面不丢。
7. 抽一个通用两栏布局类 `.split`（`1.25fr / 1fr`，≤860px 自动堆成一栏），给相机页和诊断页共用（附录 A 第 35 条）。

**验收**：`test-ui-structure.ps1` 11 项 + `grep -n "alert(\|confirm(" frontend\src\*.ts` 无命中。

---

## 4. 每个任务包做完的固定动作

顺序别换，最后两步现场才看得到效果：

```powershell
cd <仓库>\frontend
node build.mjs                      # 1) 编译 + 重算 ?v= 指纹（必须）

cd <仓库>
.\runtime\tools\test-ui-structure.ps1        # 2) 结构体检（2 秒，必跑）
.\tools\test-c1-cards.ps1                    # 3) 按任务包挑相关回归（见上表）

# 4) 铺到现场包（平台托管的是静态文件，不用重启）
Copy-Item -Path .\src\DwsEdge.Platform\wwwroot\* -Destination <包>\runtime\platform\wwwroot -Recurse -Force
.\runtime\tools\refresh-checksums.ps1 -Package <包>   # 5) 热修：只对齐校验和，不动版本号
.\runtime\tools\check-package.ps1                     # 6) 确认 7 项全 PASS
```

现场看效果：浏览器 **Ctrl+F5**（引用带指纹，普通刷新可能拿缓存）。

---

## 5. 回归脚本对照表

改哪个任务包，跑哪几个脚本（19 个脚本在 `tools\`，交付包里的同名脚本要显式传 `-SourceRuntime` / `-WorkDir`）：

| 任务包 | 必跑回归 | 项数 |
|---|---|---|
| T0 | `test-ui-structure.ps1`、`test-c1-cards.ps1` | 11 / 28 |
| T0.5 | `test-ui-structure.ps1` + **人工看导航形态**（见下） | 11 |
| T0.6 | `test-ui-structure.ps1` + 人工截图核对角标数字 | 11 |
| T1 | `test-b8-monitor.ps1`、`test-c4-stats.ps1`、`test-b9-auth.ps1` | 54 / 59 / 96 |
| T2 | `test-c1-cards.ps1`、`test-c2-wall.ps1` | 28 / 29 |
| T3 | `test-c5-config.ps1`、`test-a8-template.ps1` | 61 / 49 |
| T4 | `test-b4-downstream.ps1`、`test-b5-tcp-server.ps1`、`test-b6-http.ps1`、`test-b2-rules.ps1` | — |
| T5 | `test-b9-auth.ps1` | 96 |
| T6 | `test-c6-diag.ps1` | 49 |
| T7 | `test-ui-structure.ps1` + 文案 grep | 11 |

**已知盲区（这次踩过，记下来）**：`test-ui-structure.ps1` 只查"元素 id 在不在、应用条位置对不对、section 配平没有"，**它不测布局形态** —— "导航是横排还是侧栏"这种事它一条都报不出来。所以 T0.5 / T0.6 这类布局改动，回归全绿**不等于**做完了，必须人工截图确认。要在 T0.5 之后补一条能自动发现"导航形态与配置不一致"的断言，可以放到 T7 里顺带做（断言 `#dwsNav` 的 `data-nav` 与实际 class 一致即可）。

**已知环境依赖**：`test-c5-config.ps1` 的"相机清单写入"那一段会调 `tools\apply-config.ps1`，而该脚本在**检测到任何 `DwsEdge.Host` 进程在跑时会拒绝执行**。所以现场宿主开着的时候跑 C5，会出现 7 项失败（合法清单应用失败 / cfg 没写进去 / 接口读回 1 台而不是 3 台）。这不是代码问题 —— 先 `stop-all.ps1` 停掉宿主再跑，或者在开发机（没跑宿主）上跑。

---

## 6. 已完成与未完成的一览（截至 2026-09-22）

| | 内容 |
|---|---|
| ✅ 已完成 | **T0** 外壳与分组导航 + 全局应用条 + 主题/密度；**T1** 页头状态条 |
| 📄 本次新增文件 | `frontend\src\shell.ts`（主题/密度）、`frontend\src\statusbar.ts`（状态条） |
| 🗑 本次删除文件 | `frontend\src\hoststate.ts`（被 `statusbar.ts` 取代）、`src\DwsEdge.Platform\wwwroot\index.html.new`（误提交的旧副本，会让前端结构体检报"多余文件"） |
| ⚠️ 已知缺口 | 导航仍是**页头横排**，设计图默认的**左侧侧边栏**没做（本轮才立成 T0.5）；顶条没有品牌区 / 图标 / 角标（T0.6） |
| ⏭ 下一步 | **迭代 ①.5：T0.5 + T0.6**（侧边栏 + 品牌与图标，约 1.5 天）；之后是迭代 ② 四件套 T2 / T3 / T4 / T5 |

> 现场包 V1.0.9 已经带上 T0 + T1（热修方式：只替换 `runtime\platform\wwwroot` 下的静态文件 + `refresh-checksums.ps1`，版本号仍是 V1.0.9）。

---

## 附录 A：设计图逐项对照表

**怎么读**：左边是设计图里画了的每一个元素，右边是它落在哪个任务包。
状态三档：**已完成**（T0/T1 已实测）、**待核对**（清单里没有明确条目，动手前先花 10 分钟看一眼代码，再决定是"已具备"还是"要改"）、**待做**。

> 注意：设计图**只画了 3 个页面**（实时监控 / 相机 / 诊断）。另外 6 个页签（设备信息、历史查询、统计、输出对接、规则、系统与安全）设计图里没有，T4 / T5 只能按同一套规范自己定，没有图可对照 —— 这是设计图本身的边界，不是漏项。

| # | 设计图里的元素 | 归属任务包 | 状态 | 备注 |
|---|---|---|---|---|
| 1 | 顶条品牌区：logo + 产品名 + 版本号小字 | T0.6-1 | 待做 | 现在只有一句副标题"采集监控 · 设备信息 · 配置管理（V1）" |
| 2 | 顶条状态胶囊（设计图 4 个） | T1 | ✅ 已完成 | 我们做了 6 个，多"命令通道""实时通道"（见附录 B） |
| 3 | 顶条用户区 + 退出按钮 | T0.6-4 / T1 | 已完成 | `userBox` / `btnLogout` 已在状态条右侧，样式待随 T0.6 统一 |
| 4 | 应用条：触发模式下拉 | T0 | ✅ 已完成 | `cfgTrigger` |
| 5 | 应用条：相机清单 N 台（服务器上 N 台） | T0 | ✅ 已完成 | `applySummary` |
| 6 | 应用条：● 有未保存改动 | T0 | ✅ 已完成 | `applyDirty` |
| 7 | 应用条：一键应用（主按钮） | T0 | ✅ 已完成 | `btnApply` |
| 8 | 应用条：**上次应用：2 分钟前 · 成功（用了 8 秒）** | T7（新增条目） | 待做 | 现在只在点下去的当下显示结果，刷新页面就没了 —— 现场想知道"上次到底成没成" |
| 9 | 导航：**左侧栏 178px（默认形态）** | **T0.5** | 待做 | 就是本次立起来的那条 |
| 10 | 导航：形态切换（左侧栏 / 顶部标签） | T0.5-4 | 待做 | 写 localStorage，默认侧栏 |
| 11 | 导航：分组标签 监控 / 配置 / 系统 | T0 | ✅ 已完成 | |
| 12 | 导航：每项 16px 图标 | T0.6-2 | 待决策 | 离线环境不能用 CDN；先决策"做 / 不做" |
| 13 | 导航：项右侧数字角标（相机 17） | T0.6-3 | 待做 | 数据用已有接口 |
| 14 | 导航：当前项"浅底 + 描边"样式 | T0.5-3 | 待做 | 现在是实心蓝，大屏上偏重 |
| 15 | 实时页 KPI 5 个（含"平均过包节奏 /s"） | T2-1 | 待核对 | 现在 4 个，缺节奏 |
| 16 | 实时页 KPI 自适应网格 `minmax(140px,1fr)` | T2-1 | 待核对 | 现在是固定列数 |
| 17 | 实时页 健康条（**正常态绿底文案**） | T2-2 | 待做 | 现在没告警就整条不显示 |
| 18 | 实时页 告警条分级（critical / warning） | T2-2 | 待核对 | |
| 19 | 实时页 区块标题右侧工具（软触发 / 卡片 / 表格） | 已有 | ✅ 已完成 | `btnSoftTrigger` / `viewCards` / `viewTable` |
| 20 | 卡片墙 `auto-fill minmax(232px,1fr)` | T2-4 | 待核对 | 缩略图请求已经是 w=320 |
| 21 | 卡片：缩略图 76×60 | T2-3 | 待核对 | 与现有图片单元格尺寸可能不同 |
| 22 | 卡片：条码用等宽字体 | T2-3 | 待做 | |
| 23 | 卡片：元信息含**重量** | T2-3 | 待核对 | 重量是两次回调合并后才有的数据 |
| 24 | 卡片：三标签（读码状态 / 图 N / 下发状态）+ 更新 ×2 | T2-3 | 待核对 | |
| 25 | 相机墙 `auto-fill minmax(126px,1fr)` + 左侧 3px 状态色条 | T2-5 | 待核对 | 色条是"扫一眼找异常"的关键 |
| 26 | 相机墙：`出码 N · 在线率 N%` 一行 | T2-5 | 待核对 | |
| 27 | 相机墙：未触发态显示"出码 0 · 未触发" | T2-5 | 待做 | 别显示 0%，会被当成故障 |
| 28 | 相机页 工具四件套（扫描 / 预检 / 新增一行 / 校验格式） | T3-3 | 待做 | |
| 29 | 相机页 一行"按 IP 写最省事"的说明 | T3-4 | 待做 | |
| 30 | 相机页 表格列 `# / 接入方式 / 值 / 方位 / 预检 / 操作` | T3-5 | 待核对 | 预检已有，列与三态文案未定稿 |
| 31 | 相机页 底部"存图策略 / 配置模板"并排两栏 | T3-6 | 待做 | |
| 32 | 诊断页 工具栏（一键自检 / 导出日志包 / 上次自检摘要） | T6-4 | 待核对 | 缺"上次自检摘要" |
| 33 | 诊断页 自检表三态（通过 / 失败 / **跳过**）+ 失败带处置动作 | T6-2、T6-3 | 待做 | |
| 34 | 诊断页 右侧实时日志（来源下拉 + 跟随最新） | T6-5 | 待核对 | 日志已有，并排布局与"跟随最新"没有 |
| 35 | 通用：两栏布局 `.split`（1.25fr / 1fr）+ 860px 断点 | T7（新增条目） | 待做 | 相机页、诊断页都要用，做成一个公共类 |
| 36 | 通用：主题（跟随系统 / 深色 / 浅色） | T0 | ✅ 已完成 | 默认深色，见附录 B |
| 37 | 通用：信息密度（舒适 / 紧凑） | T0 | ✅ 已完成 | 实测字号 13→12px |
| 38 | 通用：按钮/输入框圆角、主按钮样式、表格样式 | T0 | 部分 | 表格与按钮已有，主按钮颜色有意不改（附录 B） |
| 39 | 通用：行高 token（`--dws-row: 34px`） | T7 | 可选 | 现在用单元格内距控制行高，效果等价，不一定要引入 |

**结论**：39 条里 —— **已完成 10 条**（基本都是 T0 / T1 覆盖到的）、**待核对 12 条**、**待做 14 条**、部分 1 条、待决策 1 条、可选 1 条。

**T0.5（第 9、10、14 条）是优先级最高的一组**：它是**形态**问题，不先解决的话，后面 T2 的卡片墙尺寸、T3 的两栏布局都要按"横排 + 顶部占高"的宽度去调，等侧栏一上来又要返工一次。所以迭代 ①.5 排在迭代 ② 前面。

---

## 附录 B：与设计图的有意差异（登记）

这些地方和设计图不一样，是**故意**的，不是漏项。写下来免得下次有人"顺手改回去"。

| 项 | 设计图 | 我们的决定 | 理由 |
|---|---|---|---|
| 默认主题 | 跟随系统（设计图默认 `auto`） | **默认深色** | 现场大屏一直是深色。默认跟随系统的话，Windows 是浅色模式的机器一升级整个界面就变白 —— 那是惊吓不是升级 |
| 状态胶囊数量 | 4 个（宿主 / 相机 / 下游 / 图片盘） | **6 个**（多"命令通道""实时通道"） | 命令通道决定软触发、补码能不能用；实时通道决定界面上的数据是不是活的。都属于"一票否决"，不该藏起来 |
| 主题 / 密度 / 导航形态的入口 | 在设计图自己的下拉调试面板里 | 放进页头"显示设置" | 设计图那个面板是画图工具的调试件，不是产品界面；这三项现场要能自己改 |
| 主按钮颜色 | 实心绿（`#2ea043` / `#1f883d`） | 沿用现有蓝色主色 | 不动已交付的观感，避免"升级完像换了个软件" |
| 信息密度数值 | 字号 14→12.5px、行高 34→27px | 字号 **13→12px** | 与现有界面字号基线对齐，不引入新字号档位 |

**待决策（要写结论，不能一直挂着）**

| 项 | 选项 | 决定 |
|---|---|---|
| 导航图标 | A 内联 SVG（`icons.ts`，零依赖）/ B 图标字体随 wwwroot 发布 / C 不做 | ☐ |
| 顶条版本号来源 | A 写在 `index.html`（发版手改）/ B 读 `/api/health` 已有字段 | ☐ |
| 导航默认形态 | A 侧栏（跟设计图）/ B 顶部标签（跟现状） | ☐ 建议 A |
| 行高 token | A 引入 `--row-h` / B 保持用单元格内距 | ☐ 建议 B |
